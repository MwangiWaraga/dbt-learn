version: 2

sources:
  - name: data_prep
    database: dbt-tutorial
    schema: data_prep
    tables:
      - name: stripe_payments
      - name: jaffle_shop_orders

  - name: stripe
    database: dbt-tutorial
    tables:
      - name: payment
    loaded_at_field: _batched_at
    freshness:
      error_after:
        count: 5
        period: hour
      warn_after:
        count: 3
        period: hour
