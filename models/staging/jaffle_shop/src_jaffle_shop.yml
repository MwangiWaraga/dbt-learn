version: 2

sources:
  - name: jaffle_shop
    database: dbt-tutorial
    schema: jaffle_shop
    tables:
      - name: customers
        columns:
          - name: id
            data_tests:
              - unique
              - not_null
              
      - name: orders
        columns:
          - name: id
            data_tests:
              - unique
              - not_null
        freshness: 
          error_after:
            count: 18
            period: hour
          warn_after:
            count: 10
            period: hour
        loaded_at_field: _etl_loaded_at
      